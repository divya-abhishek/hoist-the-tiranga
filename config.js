/* ==========================================================================
   Hoist the Tiranga — configuration  (this is the file the site actually reads)
   --------------------------------------------------------------------------
   Paste your Supabase values below before deployment (see README).
   The site deliberately fails closed until the shared database is configured;
   it never stores public Tirangas in this browser.

   Everything here is PUBLIC and safe to publish. Never put the service-role
   key or the admin password in this file — those live only in Supabase.
   ========================================================================== */
window.TIRANGA_CONFIG = {
  SUPABASE_URL: "https://znugchebgunanlyixare.supabase.co",
  SUPABASE_PUBLISHABLE_KEY: "sb_publishable_zz5u_VVn0uQQxq0PhZZ15w_1L1AcIT5",
  SITE_URL: "https://hoist-the-tiranga.freedev.app"
};
